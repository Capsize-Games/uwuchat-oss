"""FastSearch extension — custom search engine integration for AIRunner.

Provides an LLM agent search tool backed by the FastSearch API
(web pages, images, videos, news, books, audio, and more).
"""
