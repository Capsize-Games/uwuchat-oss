/**
 * Extension route elements for the fastsearch extension.
 *
 * Admin routes removed — functionality is now accessed via the
 * right-sidebar panel in ChatView.
 */

export const extensionRouteElements: unknown[] = [];
